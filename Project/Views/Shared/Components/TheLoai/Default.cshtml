@model IEnumerable<TheLoai>
<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Thể Loại</a>
    <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
        <ul>
            @foreach (var item in Model)
            {
                <li>
                    <a class="dropdown-item" asp-action="FilterByTheLoai"
                       asp-route-id="@item.Id">@item.Name</a>
                </li>
            }
        </ul>

    </div>


</li>
